{
  "name": "lexiguess",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install && npm run browser",
    "cli": "node cli.js",
    "server": "node static-server.js",
    "browserify": "browserify browser.js > build/browser.js",
    "browser": "npm run browserify && npm run server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "command-line-args": "^5.0.2",
    "emittery": "^0.4.1",
    "es6-promise": "^4.2.4",
    "isomorphic-fetch": "^2.2.1"
  },
  "devDependencies": {
    "browserify": "^16.2.3",
    "esmify": "^1.2.0"
  }
}
